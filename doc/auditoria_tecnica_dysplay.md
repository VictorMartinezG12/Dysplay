# Auditoría Técnica y Arquitectónica - Proyecto DysPlay

## 1. Resumen Ejecutivo
DysPlay es un proyecto web desarrollado con el framework Django (Python) y un frontend integrado utilizando Tailwind CSS y Vanilla JavaScript. El objetivo principal es ofrecer una experiencia de aprendizaje gamificada para niños, centrándose en la superación de la dislexia. La aplicación cuenta con módulos de niveles, historias interactivas, personalización de avatar, cámara inteligente y configuraciones de accesibilidad.

El proyecto presenta una estructura base sólida orientada a aplicaciones de Django, con un diseño frontend sumamente pulido, accesible y reactivo a nivel de UI, que transmite una estética premium. Sin embargo, en el backend, varios módulos están implementados únicamente a nivel de plantillas estáticas (mockups funcionales) sin modelos de base de datos ni lógica de negocio robusta detrás, lo cual es típico de una fase inicial (MVP) pero representa una deuda técnica a solventar.

---

## 2. FASE 1: Descubrimiento (Estado Actual)

### Módulos y Responsabilidades
*   **`configuracion`**: Administra las preferencias globales de accesibilidad (tamaño de fuente, dislexia, colores, volumen). Posee un modelo `ConfiguracionGlobal` y un `context_processor` para inyectar configuración en todas las plantillas.
*   **`avatar`**: Maneja la personalización del avatar del usuario. Modelos: `Avatar`, `Item`, `InventarioAvatar`, `ReaccionAvatar`. Posee lógica real para equipar ítems y un sistema de eventos JS incipiente.
*   **`niveles`**: El núcleo de juego. Modelos: `Nivel`, `MisionVocabulario`, `ProgresoEstudiante`. En la vista, interactúa con Azure Speech Services (vía `servicios.utils.evaluar_pronunciacion`) para reconocimiento de voz y guarda el progreso.
*   **`usuarios`**: Modelo `UsuarioCustom` extendiendo `AbstractUser` para agregar campos como `monedas`, `racha_dias` y `correo_tutor`.
*   **`camara_inteligente`**: Vista simulada (`camara_view`) para jugar con gestos usando la cámara. Actualmente solo renderiza una UI sin lógica backend de reconocimiento de imagen.
*   **`historias`**: Vista simulada (`historias_view`) para cuentos interactivos. No posee modelos en base de datos. Toda la información está "quemada" (hardcoded) en el template.
*   **`estadisticas`**: Vista simulada (`estadisticas_view`). No posee modelos propios, pero debería cruzar datos de `ProgresoEstudiante`.
*   **`servicios`**: Módulo utilitario sin modelos. Contiene `utils.py` con la lógica para conectarse a Azure Speech.
*   **`desafio` / `core`**: `core` agrupa las configuraciones base de Django y `desafio` está vacío actualmente.

**Dependencias y Flujo de datos:**
Las apps están integradas principalmente en los templates a través de enlaces directos. El `context_processor` del avatar inyecta en el DOM el estado actual del personaje y las reacciones, permitiendo al frontend actualizar su UI basado en las preferencias sin consultar repetidamente la base de datos por Ajax.

---

## 3. FASE 2: Arquitectura

### Evaluación de Diseño
*   **Modularidad**: **Alta**. Django impone una separación por aplicaciones, y el proyecto la respeta. Cada funcionalidad está aislada en su propia app.
*   **Cohesión**: **Media-Alta**. Las apps como `avatar` y `configuracion` tienen responsabilidades claras y agrupadas.
*   **Acoplamiento**: **Bajo-Medio**. El acoplamiento más fuerte se da en los templates (UI unificada) y la dependencia global al usuario activo (`request.user`).
*   **SRP (Single Responsibility Principle)**: Bien aplicado a nivel de apps. Sin embargo, en `niveles/views.py`, la función `guardar_progreso` mezcla la recepción del archivo, la validación, la llamada al servicio externo (Azure) y la lógica de respuesta JSON/Redirect.

### ¿Qué está bien diseñado?
*   El uso de **Context Processors** para `configuracion` y `avatar` permite mantener la UI persistente del avatar sin tener que llamar a la base de datos en cada vista.
*   La encapsulación del servicio de IA en `servicios/utils.py` evita ensuciar las vistas con configuración de la API de Azure.
*   Personalización del modelo de usuario mediante `AbstractUser`.

### ¿Qué está mal diseñado o podría romperse?
*   Falta de serializadores o un API Framework estructurado (DRF). El frontend envía datos vía FormData y `niveles/views.py` devuelve `JsonResponse`. Esto es difícil de escalar si se desea separar completamente el frontend.
*   Dependencias fuertes de UI (Mockups). Las apps de `historias`, `camara_inteligente` y `estadisticas` tienen código HTML masivo que simula bases de datos.
*   En `configuracion/views.py`, los cambios se procesan mediante `request.POST.get()` manual y exhaustivo, en lugar de usar Django Forms (`ModelForm`).

---

## 4. FASE 3: Base de Datos

### Relaciones y Modelos
*   `Avatar` tiene un `OneToOneField` con `User`. Correcto.
*   `InventarioAvatar` usa una relación intermedia `ForeignKey` a `Avatar` e `Item`, actuando como una relación ManyToMany explícita con campos adicionales (`equipado`, `desbloqueado`). Muy bien estructurado.
*   `ConfiguracionGlobal` tiene un `OneToOneField` con `User`. Correcto.
*   `ProgresoEstudiante` se relaciona mediante `OneToOneField` a `User`, pero el nombre `Usuario` es muy genérico, podría ser confuso.
*   `MisionVocabulario` tiene un `ForeignKey` hacia `Nivel`. Correcto.

### Riesgos y Posibles Cuellos de Botella
*   **Duplicación de datos implícita**: El saldo del usuario (`monedas`) y la `racha_dias` se guardan en el modelo `UsuarioCustom`. Si el proyecto crece, es mejor tener un modelo `PerfilEstudiante` / `ProgresoGlobal` o similar para separar la lógica de autenticación de las métricas de gamificación.
*   **Falta de modelos**: Las historias interactivas y estadísticas no existen en DB. Al crecer el proyecto, habrá que migrar todo el texto hardcodeado a modelos relacionales.

---

## 5. FASE 4: Frontend

*   **TailwindCSS**: Utilizado vía CDN (`<script src="https://cdn.tailwindcss.com"></script>`). Esto está bien para desarrollo, pero en producción es lento y no está optimizado. Debería integrarse mediante Node.js/PostCSS para generar un bundle minimizado.
*   **Diseño UI/UX**: Excepcional. Cumple de sobra con el requerimiento de tener un diseño "premium", amigable, infantil y enfocado a accesibilidad. Se usan animaciones fluidas, paletas de colores adecuadas y Lucide Icons.
*   **JavaScript**: El uso de Vanilla JS es funcional, pero el código de grabación de audio en `niveles.html` (`toggleRecording`) mezcla lógica de Audio API de bajo nivel, DOM manipulación y peticiones Fetch. Esto puede ser propenso a errores en navegadores de móviles no estandarizados (iOS Safari).
*   **Reutilización**: Se está repitiendo mucho "boilerplate" de Tailwind y layouts (`<header>`, navegación lateral) dentro de plantillas individuales en vez de aprovechar completamente los bloques dinámicos del `base.html`.

---

## 6. FASE 5: Seguridad

*   **Login Required**: Todas las vistas funcionales usan el decorador `@login_required`. Excelente.
*   **Validaciones Backend**: Faltan validaciones de tipo y saneamiento riguroso. En `guardar_progreso`, no se valida si el archivo enviado es efectivamente un `.wav` real o malicioso, o si excede el tamaño máximo permitido. Solo se confía en la extensión temporal.
*   **File Uploads**: `Item.imagen` utiliza `ImageField`. Está bien, pero sería recomendable restringir formatos y pesos.
*   **Exposición de Logs**: En `niveles/views.py` se captura `Exception as e` y se devuelve el mensaje de error directamente en un `JsonResponse` `({'status': 'error', 'message': str(e)})`. Esto podría exponer información sensible del servidor (ej: rutas del sistema operativo) al cliente.

---

## 7. FASE 6: Escalabilidad

*   **Facilidad para nuevas apps**: Muy alta. Django permite añadir módulos rápidamente.
*   **Nuevas vistas/modelos**: Facilidad alta por la naturaleza monolítica y modular actual.
*   **Escalabilidad Frontend**: Media. Debido al uso extensivo de clases CSS en línea y JavaScript mezclado en HTML, el código frontend se está volviendo espagueti. Migrar a React/Vue o un sistema de componentes más formal (Alpine.js, HTMX) ayudaría mucho en el futuro.
*   **Procesamiento Asíncrono**: Azure Speech es llamado sincrónicamente dentro de una petición HTTP (`recognize_once_async().get()`). Bajo carga de muchos usuarios concurrentes, esto congelará workers del servidor web (Gunicorn/WSGI). Debe moverse a tareas en background (Celery) o usar WebSockets/Asgi.

---

## 8. FASE 7: Matriz de Riesgos

| Riesgo | Impacto | Probabilidad | Prioridad |
| ------ | ------- | ------------ | --------- |
| Tailwind cargado por CDN en Producción | Medio | Alta | Alta |
| Bloqueo del servidor web por peticiones sincrónicas a Azure (Azure Timeout) | Crítico | Media | Alta |
| Exposición de errores del backend vía JSON (StackTrace leak) | Alto | Media | Alta |
| Código JS y plantillas inmanejables por excesiva longitud y repetición | Medio | Alta | Media |
| Archivos de audio subidos sin validación de tipo real o peso máximo | Alto | Baja | Media |
| Datos dinámicos estáticos (hardcodeados) en Historias y Estadísticas | Bajo | Alta | Baja |

---

## 9. FASE 8: Roadmap Técnico Propuesto

### 1. Correcciones Críticas (Inmediato)
1.  **Migrar Tailwind CDN a Build Pipeline**: Configurar Node.js y compilar el archivo CSS estático oficial para producción.
2.  **Sanitización de Errores y Seguridad de Archivos**: Limitar el tamaño de archivos subidos en Azure Speech y devolver mensajes de error genéricos al frontend, registrando los errores reales en logs (`logging`).
3.  **Refactor de `niveles/views.py`**: Separar la lógica de subida y conexión a Azure en servicios independientes; no dejar que la vista maneje limpieza temporal de OS si no es estrictamente necesario, o usar `InMemoryUploadedFile`.

### 2. Mejoras Recomendadas (A corto plazo)
1.  **Refactor Frontend (Django Templates + JS)**: Separar el código JS en archivos `.js` ubicados en la carpeta estática (`/static/js/`), y usar bloques modulares para los layouts (Header, Footer) de forma de no repetir el código en las vistas HTML.
2.  **Uso de Django Forms**: En `configuracion/views.py`, implementar un `ConfiguracionForm(forms.ModelForm)` para aprovechar las validaciones de Django al actualizar la base de datos.
3.  **Modelado de Módulos Faltantes**: Diseñar y crear `models.py` para `historias`, `estadisticas` y `camara_inteligente` para abandonar la etapa Mockup.

### 3. Mejoras Opcionales (A medio plazo)
1.  **Framework de API (Django Rest Framework o Ninja API)**: Exponer la comunicación de Reactividad del Frontend (Guardar Progreso, Cambiar Ropa del Avatar) a través de un API estandarizado en vez de endpoints construidos "a mano" con `JsonResponse`.
2.  **Integración de HTMX / AlpineJS**: Para lograr interactividad compleja (como el caso del Avatar) sin escribir tanto Vanilla JS engorroso.

### 4. Refactorizaciones Futuras (A largo plazo)
1.  **Desacople Arquitectónico (Frontend SPA)**: Migrar el frontend a Next.js / Vue.js conectándose al backend de Django vía APIs REST o GraphQL, aprovechando todo el diseño logrado.
2.  **Celery y Redis**: Para tareas pesadas de IA, enviar el audio al servidor, devolver un "Task ID" al cliente, y hacer polling o WebSockets para recibir la respuesta de Azure de forma asincrónica real y no bloquear el request principal.

---
*Fin del reporte de Auditoría.*

## Actualizaciones

Esta sección registra cambios relevantes posteriores al reporte original.
El agente `documentador-dysplay` agrega una entrada nueva al cerrar cada
módulo del Master Plan, con el formato:

```
### Módulo <X> — <nombre del módulo> (<fecha YYYY-MM-DD>)
- Resumen: <3-5 líneas de lo implementado>
- Archivos tocados: <lista breve>
- Checklist de calidad: <cumplido / pendiente: detalle>
```

### Pre-Master Plan (sin fecha registrada)
- Se agregó la API de Google Cloud Vision (módulo de cámara inteligente).
- No se modificó nada más en esa actualización.

### Módulo A — Correcciones Críticas de Infraestructura (2026-06-13)
- Resumen: A.1 — Tailwind migrado de CDN a build pipeline (tailwind.config.js, postcss.config.js, static/css/output.css ~46KB, CDN eliminado de 6 templates, Lucide intacto). A.2 — niveles/views.py refactorizado: nuevo niveles/services.py (procesar_audio_subido con validación python-magic, evaluar_pronunciacion_azure, guardar_progreso_estudiante, calcular_recompensas); vista guardar_progreso reducida a ~8 líneas, sin exponer str(e), logging con exc_info; CASO B ahora persiste progreso real. A.3 — JS de niveles.html (208 líneas) extraído a static/js/niveles.js (JSDoc, pipeline WAV preservado), datos Django→JS vía json_script y data-* attributes.
- Archivos tocados: package.json, tailwind.config.js, postcss.config.js, static/css/input.css, static/css/output.css, templates/base.html, camara_inteligente/templates/camara_inteligente/camara.html, avatar/templates/avatar/avatar.html, historias/templates/historias/historias.html, estadisticas/templates/estadisticas/estadisticas.html, niveles/templates/niveles/niveles.html, niveles/views.py, niveles/services.py (nuevo), niveles/tests.py, static/js/niveles.js (nuevo), requirements.txt (+python-magic==0.4.27).
- Checklist de calidad: cumplido (14/14 tests OK, makemigrations sin cambios, @login_required sin fugas, JS sin inline salvo json_script, admin con list_display, flujo UX de niveles.html respetado).
- Veredicto arquitecto: APROBADO CON SEGUIMIENTOS.
- Deuda/seguimientos para módulos futuros:
  - 5 templates standalone (avatar, camara, estadisticas, niveles, historias) usan el token `primaryFijo` (#3B82F6, azul fijo que no sigue el tema dinámico). Revisión visual humana pendiente; decidir en Módulo I si deben migrar a `primary` dinámico.
  - Regla "monedas = nivel.puntos_recompensa si score>=70" vive en niveles/services.py:calcular_recompensas. El Módulo B debe centralizarla en el sistema de recompensas unificado y hacer que niveles la consuma, sin duplicar fuente de verdad.
  - racha_dias queda pendiente para el Módulo B, que deberá añadir el campo de fecha de última actividad necesario.
  - static/css/output.css pesa ~46.1KB de 50KB máximo. Vigilar tras el Módulo D (mapa de niveles) y considerar cssnano si se acerca al límite.
  - Preexistente (no de Módulo A): camara_inteligente/tests.py es un script con efectos de red real (Google Vision), no un TestCase Django; contamina la salida de manage.py test. Anotado para Módulo G.

### Módulo B — Sistema de Recompensas Unificado (2026-06-13)
- Resumen: Se creó la app `recompensas` con 7 modelos (TipoInsignia, Insignia, Mascota, MascotaUsuario, Coleccionable, ColeccionableUsuario, EventoEspecial). recompensas/services.py implementa otorgar_monedas (atómico con F()/select_for_update), verificar_y_otorgar_insignias, obtener_insignias_pendientes, actualizar_racha (usa el campo ultima_fecha_conexion ya existente en UsuarioCustom, sin cambios de esquema) y get_evento_activo. recompensas/signals.py conecta post_save de ProgresoEstudiante -> verificar_y_otorgar_insignias, y user_logged_in -> actualizar_racha (desvío aprobado respecto al Master Plan: la racha se actualiza al iniciar sesión, no al guardar progreso, para evitar escrituras repetidas; cubre también login vía Google OAuth/allauth). niveles/services.py:calcular_recompensas fue refactorizado para delegar el otorgamiento de monedas en recompensas.services.otorgar_monedas, conservando firma y retorno exactos. avatar/context_processors.py:avatar_global se extendió con 5 claves nuevas (insignias_pendientes sin marcar, mascota_usuario, monedas_usuario, racha_dias, evento_activo) sin modificar las 3 claves existentes (avatar_user, avatar_equipados, reacciones_json), maneja usuario anónimo. Se cargaron fixtures de ejemplo (insignias, mascotas, coleccionables). Iteración backend-only; UI de insignias/mascotas queda para Módulo C.
- Archivos tocados: recompensas/models.py (nuevo), recompensas/admin.py (nuevo), recompensas/apps.py (nuevo), recompensas/services.py (nuevo), recompensas/signals.py (nuevo), recompensas/views.py (nuevo, stub sin uso), recompensas/tests.py (nuevo, 23 tests), recompensas/migrations/0001_initial.py (nuevo), recompensas/fixtures/tipos_insignia.json (nuevo), recompensas/fixtures/mascotas.json (nuevo), recompensas/fixtures/coleccionables.json (nuevo), core/settings.py (INSTALLED_APPS), niveles/services.py (calcular_recompensas), avatar/context_processors.py (avatar_global).
- Checklist de calidad: cumplido (23 tests nuevos + 36 tests de suite completa OK excluyendo camara_inteligente, makemigrations --check limpio, admin con list_display en los 7 modelos, sin str(e) expuesto, monedas atómicas y server-side, insignias automáticas vía signal sin recursión, 13 tests preexistentes de niveles sin romper).
- Veredicto arquitecto: APROBADO CON SEGUIMIENTOS.
- Deuda/seguimientos para módulos futuros:
  - Módulo C (o el que implemente UI de avatar/insignias): invocar obtener_insignias_pendientes desde una vista para marcar mostrada=True y disparar animación; consumir las 5 claves nuevas del context processor (insignias_pendientes, mascota_usuario, monedas_usuario, racha_dias, evento_activo).
  - Módulo F (Historias): implementar el criterio de insignia "historias_10" (hoy placeholder con criterio_cumplido=False) cuando historias exponga un contador de historias completadas.
  - Lag menor conocido: las insignias de racha (racha_7/racha_30) se otorgan en la siguiente acción de progreso del día, no en el instante del login, porque se evalúan en el post_save de ProgresoEstudiante leyendo racha_dias ya actualizada por el login previo. Aceptable para la UX actual; reevaluar solo si se requiere feedback inmediato.
  - recompensas/views.py es un stub intencional sin uso, reservado para futuras vistas de insignias/mascotas/tienda.

### Módulo C — Avatar Avanzado — Compañero de Aventura (2026-06-14)
- Resumen: C.1 — Avatar extendido con frase_bienvenida y personalidad (animado/tranquilo/gracioso); Item ampliado con categoria 'habitacion'/'fondo' (sin tocar choices previos), precio_monedas y evento_especial (FK recompensas.EventoEspecial, SET_NULL); nuevo modelo CasaAvatar (OneToOne con Avatar, slots cama/cuadro/alfombra/lampara como FK a Item). C.2 — avatar/reactions.py con diccionario REACCIONES (7 tipos de evento) y obtener_reaccion(); context processor avatar_global agrega la clave nueva avatar_frase_contextual (las 8 claves previas intactas); avatar_events.js conectado por primera vez vía script tags JSON en base.html (reacciones_json y avatar_frase_contextual), con nuevo método mostrarFraseContextual(). C.3 — Casa del Avatar: avatar/casa.html rediseñado (habitación visual con 4 slots, sección "Mis ítems" y "Tienda"), static/js/avatar/casa.js para compra/colocación; nuevas vistas casa_avatar, comprar_item, colocar_item y rutas avatar:casa/comprar_item/colocar_item (avatar:personalizar intacta); compra de ítems usa recompensas.services.cobrar_monedas (nueva función atómica + SaldoInsuficienteError), reutilizando el sistema unificado de recompensas del Módulo B.
- Archivos tocados: avatar/models.py, avatar/admin.py, avatar/views.py, avatar/urls.py, avatar/context_processors.py, avatar/reactions.py (nuevo), avatar/services.py (nuevo), avatar/tests.py (nuevo, 31 tests), avatar/migrations/0003_avatar_frase_bienvenida_avatar_personalidad_and_more.py (nuevo), avatar/templates/avatar/casa.html, avatar/templates/avatar/personalizar.html, static/js/avatar_events.js, static/js/avatar/casa.js (nuevo), templates/base.html, recompensas/services.py.
- Checklist de calidad: cumplido (31 tests nuevos de avatar + 23 de recompensas = 54 OK, suite completa 68 tests OK sin regresiones; makemigrations sin pendientes; @login_required en las 4 vistas de avatar sin exponer str(e); JS externo (avatar_events.js, avatar/casa.js) sin inline; modelos nuevos/extendidos en admin con list_display; flujo UX existente de personalizar.html respetado, solo se agregó el botón "Mi Casa"; compra de ítems de habitación validada server-side vía cobrar_monedas atómico).
- Deuda/seguimientos para módulos futuros:
  - Item.categoria mantiene los choices preexistentes ('cabello','ropa_superior','ropa_inferior','calzado','accesorio','mascota','mueble','decoracion','trofeo') más los nuevos 'habitacion' y 'fondo' del Master Plan; no se migró/renombró la lista completa del Master Plan para evitar migración destructiva de datos (decisión del arquitecto). Revisar si en un módulo futuro conviene unificar nomenclatura.
  - No hay fixtures de ítems con categoria='habitacion'/'fondo' ni precio_monedas > 0 todavía; la Casa y la Tienda funcionan correctamente con listas vacías, pero se recomienda cargar fixtures de ejemplo en un módulo futuro (o antes de demo) para mostrar el flujo de compra real.
  - ReaccionAvatar (modelo BD, admin-editable) coexiste con avatar/reactions.py (dict de variantes en código); avatar_frase_contextual usa solo reactions.py. Si se desea que el admin controle también la frase contextual, evaluar unificación en un módulo futuro.

### Módulo D — Mapa de Niveles (2026-06-14)
- Resumen: D.1 — Reemplazó lista de niveles por Mapa de Aventura visual (SVG, 5 zonas temáticas: Bosque Encantado, Montaña de las Letras, Valle de las Sílabas, Castillo de las Palabras, Reino de la Lectura); data-driven vía obtener_mapa_aventura(usuario). Modelo Nivel ganó campos aditivos zona, orden_en_zona, narrativa_intro (migraciones 0003 esquema + 0004 backfill no destructivo de 3 niveles existentes, reverso noop). Por decisión de alcance, no se sembraron 17 niveles restantes del Master Plan: 4 zonas sin niveles muestran "Próximamente" (desbloqueadas) o "Bloqueada" (no), manteniendo mapa data-driven. D.2 — procesar_intento_nivel centraliza en services.py flujo evaluación (Azure → progreso → recompensas → reacción avatar); evaluar_pronunciacion ahora retorna desglose palabras (score por palabra, extraído de JSON Azure con granularity=Phoneme). UI resalta cada palabra verde/rojo según score >=70. Reacción avatar (construir_reaccion_avatar) se entrega como datos planos {tipo, mensaje} en JSON, renderizados en #resultado-mensaje-avatar sin depender de AvatarSystem/AVATAR_EVENT. D.3 — Cada zona/nivel puede tener narrativa_intro; si no vacío, muestra en modal #modal-narrativa antes de iniciar primer ejercicio. Solo nivel 1 (Bosque Encantado) tiene narrativa cargada por ahora.
- Archivos tocados: niveles/models.py, niveles/migrations/0003_nivel_narrativa_intro_nivel_orden_en_zona_nivel_zona.py, niveles/migrations/0004_datos_zona_mapa_aventura.py, niveles/admin.py, niveles/services.py, niveles/views.py, servicios/utils.py, niveles/templates/niveles/mapa.html (nuevo), niveles/templates/niveles/niveles.html, static/js/niveles.js, niveles/tests.py.
- Checklist de calidad: cumplido (83/83 tests OK incluyendo 15 nuevos de mapa/reacciones/progreso; migraciones aplicadas; @login_required sin fuga errores; JS sin inline; admin con list_display; flujo UX respetado; recompensas validadas server-side).

### Módulo E — Desafío Diario (2026-06-14)
- Resumen: Nueva app `desafio` (template standalone tipo niveles.html). Modelos: `ConfiguracionDesafio` (singleton pk=1, narrativa/zona activa/meta diaria/recompensa base), `DesafioDiario` (fecha única, M2M a `MisionVocabulario` para ejercicios obligatorios/opcionales, recompensa en monedas y coleccionable opcional) y `ProgresoDesafio` (progreso por usuario/día, unique_together). `services.obtener_o_crear_desafio_de_hoy` genera el desafío del día (1 obligatorio + hasta 3 opcionales al azar entre todas las `MisionVocabulario`); `obtener_narrativa_del_dia` compone el arco narrativo continuo ("Las letras mágicas desaparecieron del reino...") según la zona activa. `procesar_intento_desafio` reutiliza `niveles.services.procesar_audio_subido`/`evaluar_pronunciacion_azure` (mismo umbral `UMBRAL_SUPERACION_NIVEL`=70); al completar todos los ejercicios del día otorga monedas vía `recompensas.otorgar_monedas`, un coleccionable aleatorio no poseído (`otorgar_coleccionable_aleatorio`) y evalúa insignias. Se agregó el criterio `desafio_diario` a `TipoInsignia.CRITERIO_CHOICES` + insignia "Aventurero Diario" (migración de datos + fixture). `avatar/reactions.py` ganó el tipo `desafio_completado`. Frontend: `desafio.html` (vistas inicio/ejercicio/loading/resultado + resumen final con monedas/coleccionable/insignia, countdown de 24h cuando ya está completado) y `static/js/desafio/desafio.js` (grabación/codificación WAV reutilizada de niveles.js, CSRF vía `{% csrf_token %}` + `document.querySelector('[name=csrfmiddlewaretoken]')`). Se agregó la 6ª tarjeta "Desafío del Día" en `templates/home.html`. Migración de datos aditiva en `niveles` agrega 3 `MisionVocabulario` (niveles 1, 2 y 3) para que el desafío siempre tenga ejercicios suficientes.
- Archivos tocados: desafio/models.py, desafio/services.py, desafio/views.py, desafio/urls.py, desafio/admin.py, desafio/tests.py, desafio/migrations/0001_initial.py, desafio/templates/desafio/desafio.html, static/js/desafio/desafio.js, core/urls.py, templates/home.html, recompensas/models.py, recompensas/services.py, recompensas/migrations/0002_alter_tipoinsignia_criterio.py, recompensas/migrations/0003_datos_insignia_desafio_diario.py, recompensas/fixtures/tipos_insignia.json, avatar/reactions.py, avatar/tests.py, niveles/migrations/0005_datos_misiones_desafio_diario.py, static/css/output.css (rebuild).
- Checklist de calidad: cumplido (16 tests nuevos de `desafio` + suite completa 99/99 OK sin regresiones tras actualizar `avatar.tests.ReactionsTests` por el nuevo tipo de reacción; migraciones de esquema y de datos aplicadas; vista `evaluar_ejercicio` con `@login_required`, sin exponer `str(e)`; JS externo sin inline salvo `json_script`; los 3 modelos nuevos registrados en admin con `list_display`; flujo UX (vistas inicio/ejercicio/resultado, mic/WAV, reacciones del avatar) calcado del patrón de niveles.html; monedas/coleccionable/insignia otorgados y validados 100% server-side).
- Deuda/seguimientos para módulos futuros:
  - `desafio.html` usa el token `primaryFijo` (#3B82F6), consistente con las demás plantillas standalone (decisión heredada del Módulo A); revisar junto con el resto en el Módulo I si se unifica el theming dinámico.
  - El desafío del día elige ejercicios al azar entre *todas* las `MisionVocabulario` existentes, sin filtrar por nivel/zona del usuario. Cuando el Módulo D (o uno futuro) siembre los ~20 niveles restantes del Master Plan, conviene revisar si el desafío debe acotarse a niveles ya desbloqueados por el estudiante.
  - `DesafioDiario.recompensa_coleccionable` es opcional vía admin (no hay fixtures con uno asignado); por defecto se sortea entre todos los `Coleccionable` no poseídos. Cargar `recompensa_coleccionable` por fecha es una mejora editorial futura, no bloqueante.

### Módulo F — Historias Interactivas (2026-06-14)
- Resumen: Migró el módulo `historias` (antes hardcodeado en el HTML) a datos en base de datos, respetando el flujo HU-05. Modelos nuevos: `Historia` (incluye `recompensa_monedas` y `orden` para desbloqueo secuencial, igual que `niveles`), `FragmentoHistoria` (narración + pregunta interactiva opcional, `unique_together` historia/orden), `OpcionRespuesta` (con `fragmento_siguiente` para ramificación narrativa) y `ProgresoHistoria` (guardado automático del fragmento actual, `unique_together` usuario/historia). `services.obtener_historias_disponibles` calcula el estado del carrusel (completada/disponible/bloqueada) con desbloqueo secuencial por `Historia.orden`, igual patrón que el mapa de niveles. `construir_estado_lectura`/`procesar_respuesta_fragmento` orquestan la lectura: evaluación 'elegir' (ramificación vía `fragmento_siguiente` u orden por defecto), 'escribir' (comparación normalizada sin tildes/mayúsculas vía `normalizar_texto`, sin Azure Language) y 'pronunciar' (reutiliza `niveles.services.procesar_audio_subido`/`evaluar_pronunciacion_azure` y el umbral `UMBRAL_SUPERACION_NIVEL`=70). Al completar una historia se otorgan `recompensa_monedas` vía `recompensas.otorgar_monedas` y se evalúan insignias, incluyendo el criterio `historias_10` (pendiente desde el Módulo B), ahora resuelto con `obtener_historias_completadas`. Reacciones del avatar (`historia_completada`, `pronunciacion_correcta`/`incorrecta`) reutilizan `avatar.reactions.obtener_reaccion`. Frontend: `historias.html` rediseñado en dos vistas (selección del carrusel data-driven y lectura con narración/pausa-pregunta/pensando/desenlace) sobre el mismo patrón visual que `desafio.html`/`niveles.html`; `static/js/historias/historias.js` (grabación/codificación WAV reutilizada para 'pronunciar', TTS para narración sin audio grabado, fetch a `historias_evaluar` con CSRF). Se cargó `historias/fixtures/historias_inicial.json` con 3 historias de ejemplo (una con ramificación 'elegir', una con 'escribir' y una con 'pronunciar').
- Archivos tocados: historias/models.py, historias/admin.py, historias/services.py (nuevo), historias/views.py, historias/urls.py, historias/tests.py, historias/migrations/0001_initial.py (nuevo), historias/fixtures/historias_inicial.json (nuevo), historias/templates/historias/historias.html, static/js/historias/historias.js (nuevo), recompensas/services.py (criterio `historias_10`), static/css/output.css (rebuild).
- Checklist de calidad: cumplido (21 tests nuevos de `historias` + suite completa 120/120 OK sin regresiones; migración aplicada; vistas `historias_view`/`evaluar_fragmento` con `@login_required`, sin exponer `str(e)`; JS externo sin inline salvo `json_script`; los 4 modelos registrados en admin con `list_display`/inlines; flujo UX del carrusel y de la lectura respetado; monedas e insignia (`historias_10`) otorgadas y validadas 100% server-side).
- Deuda/seguimientos para módulos futuros:
  - `historias.html` usa el token `primaryFijo` (#3B82F6), consistente con el resto de plantillas standalone (decisión heredada del Módulo A); revisar junto con las demás en el Módulo I.
  - Las 3 historias de la fixture no tienen `portada` ni `audio_narracion` cargados (campos opcionales); el carrusel y la lectura funcionan con los fallbacks (icono genérico y TTS del navegador). Cargar assets reales es una mejora editorial futura, no bloqueante.
  - La evaluación de respuestas escritas usa comparación normalizada (sin tildes/mayúsculas, igualdad o substring) contra `OpcionRespuesta.texto`, en vez de Azure Language (no disponible). Si en el futuro se integra Azure Language, `services._evaluar_escribir` es el único punto a modificar.

### Módulo G — Cámara Inteligente (2026-06-14)
- Resumen: Implementó el flujo real de G.1/G.2 sobre `camara_inteligente`, antes una vista simulada (deuda anotada desde el Módulo A). Modelo nuevo `FraseTemplate` (objeto_keyword, frase_plantilla, nivel_dificultad 1-5, recompensa_monedas), con fixture `frases_template.json` de 60 frases cubriendo 60 objetos cotidianos en los 5 niveles de dificultad (recompensa = nivel_dificultad × 5). `servicios/utils.py` ganó `analizar_imagen_google_vision` (envoltorio REST de Google Cloud Vision, `LABEL_DETECTION`, mismo patrón que `evaluar_pronunciacion` para Azure). `camara_inteligente/services.py` (nuevo) implementa: `TRADUCCION_OBJETOS` (diccionario interno de ~70 etiquetas en inglés de Vision → 60 palabras en español, según lo decidido en Q1); `validar_imagen_base64` (decodifica la *data URL*, valida tamaño y tipo real JPEG/PNG con python-magic, mismo patrón que la validación de audio WAV); `_nivel_dificultad_usuario` (deriva el nivel 1-5 de `niveles.ProgresoEstudiante.nivel_actual.numero`, acotado, default 1, según Q3); `generar_frase_objeto` (recorre las etiquetas de Vision por confianza, traduce, filtra `FraseTemplate` por `nivel_dificultad__lte` el del estudiante con fallback a cualquier nivel del objeto, y fallback final a una frase genérica si no hay traducción/plantilla); `procesar_captura_imagen` (orquesta validación → Vision → frase, endpoint `/camara/capturar/`) y `procesar_evaluacion_pronunciacion` (orquesta `niveles.services.procesar_audio_subido`/`evaluar_pronunciacion_azure`, umbral `UMBRAL_SUPERACION_NIVEL`=70, otorga monedas vía `recompensas.otorgar_monedas` según la `FraseTemplate.recompensa_monedas` de la frase exacta evaluada —monto variable según `nivel_dificultad`, según Q2—, y construye la reacción del avatar reutilizando los tipos existentes `pronunciacion_correcta`/`incorrecta`, endpoint `/camara/evaluar/`). Vistas nuevas `capturar_objeto`/`evaluar_pronunciacion` (`@login_required`, delgadas, sin exponer `str(e)`) y rutas `camara_capturar`/`camara_evaluar`. Frontend: `camara.html` reemplazó el visor simulado y el script inline por `<video>` + `getUserMedia` (cámara), captura de frame a `canvas.toDataURL('image/jpeg')`, y reutiliza el pipeline de grabación/codificación WAV (`static/js/camara_inteligente/camara.js`, mismo patrón que `desafio.js`/`historias.js`) para evaluar la pronunciación; resultado muestra precisión, mensaje del avatar y monedas ganadas (si hubo).
- Desvío aprobado respecto al Master Plan: el Master Plan ubica `generar_frase_objeto`/`TRADUCCION_OBJETOS` en `servicios/utils.py`. Se mantuvieron en `camara_inteligente/services.py` para preservar el patrón de capas establecido (cada app tiene su `services.py`; `servicios/utils.py` queda reservado a envoltorios puros de APIs de IA externas sin dependencias de modelos de otras apps) y evitar un import circular (`servicios` necesitaría `camara_inteligente.models.FraseTemplate`, mientras `camara_inteligente` ya depende de `servicios`/`niveles.services`). Precedente: mismo tipo de desvío aprobado en el Módulo B.
- Archivos tocados: camara_inteligente/models.py, camara_inteligente/admin.py, camara_inteligente/services.py (nuevo), camara_inteligente/views.py, camara_inteligente/urls.py, camara_inteligente/tests.py, camara_inteligente/migrations/0001_initial.py (nuevo), camara_inteligente/fixtures/frases_template.json (nuevo), camara_inteligente/templates/camara_inteligente/camara.html, static/js/camara_inteligente/camara.js (nuevo), servicios/utils.py, static/css/output.css (rebuild).
- Checklist de calidad: cumplido (21 tests nuevos de `camara_inteligente` + suite completa 140/140 OK sin regresiones; migración aplicada y fixture cargada; vistas `capturar_objeto`/`evaluar_pronunciacion` con `@login_required`, sin exponer `str(e)`; JS externo sin inline salvo `json_script`; `FraseTemplate` registrado en admin con `list_display`/`list_filter`/`search_fields`; flujo UX de `camara.html` (visor → análisis objeto → ejercicio → análisis voz → resultado) respetado; monedas otorgadas y validadas 100% server-side mediante lookup exacto de `FraseTemplate.frase_plantilla`).
- Deuda/seguimientos para módulos futuros:
  - Se reemplazó por completo `camara_inteligente/tests.py` (antes un script con efectos de red real contra Google Vision, anotado como deuda desde el Módulo A) por `TestCase` con mocks.
  - `camara.html` usa el token `primaryFijo` (#3B82F6), consistente con el resto de plantillas standalone (decisión heredada del Módulo A); revisar junto con las demás en el Módulo I.
  - El icono dinámico del objeto detectado en `#view-exercise` quedó genérico (`sparkles`); si se desea un icono específico por objeto, se necesitaría un segundo diccionario `objeto_keyword` → icono de Lucide.

### Módulo H — Estadísticas (2026-06-14)
- Resumen: Implementó el panel de Estadísticas con datos 100% reales, eliminando los hardcodeados ("Leo Explorer", "Nivel 12", "70%", etc.). Nuevo modelo `RegistroActividad` (app `estadisticas`, migración `0001_initial`) que registra cada evaluación de pronunciación exitosa con `usuario`, `fecha`, `tipo_actividad` (nivel/historia/desafio/camara), `zona` opcional y `score`. Integrado en 4 puntos de evaluación: `niveles.services.procesar_intento_nivel`, `historias.services._evaluar_pronunciar`, `desafio.services.procesar_intento_desafio` y `camara_inteligente.services.procesar_evaluacion_pronunciacion`. `estadisticas/services.py` (nuevo) construye contexto completo vía `construir_contexto_estadisticas(usuario)`: resumen de progreso (nivel/racha/niveles superados/progreso general/palabras pronunciadas/historias completadas/progreso por zona/áreas de mejora), gráfico de actividad semanal, calendario de progreso (4 semanas), y galería de coleccionables con insignias. Vista `estadisticas_view` delgada (~3 líneas) llama a `services` sin exponer errores. Plantilla `estadisticas.html` actualizada con datos reales manteniendo flujo UX original. No otorga recompensas nuevas (solo lectura).
- Archivos tocados: estadisticas/models.py, estadisticas/migrations/0001_initial.py, estadisticas/services.py (nuevo), estadisticas/views.py, estadisticas/admin.py, estadisticas/templates/estadisticas/estadisticas.html, estadisticas/tests.py (23 tests nuevos), niveles/services.py, historias/services.py, desafio/services.py, camara_inteligente/services.py.
- Checklist de calidad: cumplido (23 tests nuevos + suite completa 163/163 OK sin regresiones; migración aplicada; vista `estadisticas_view` con `@login_required` sin exponer `str(e)`; JS externo sin inline; `RegistroActividad` en admin con `list_display`/`list_filter`; flujo UX respetado; recompensas no aplica para módulo de solo lectura).
- Deuda/seguimientos para módulos futuros:
  - `estadisticas.html` usa el token `primaryFijo` (#3B82F6), consistente con plantillas standalone (Módulo I).
  - `construir_areas_mejora` solo evalúa zonas con `RegistroActividad` existentes; comportamiento esperado (estudiantes nuevos sin actividad no tienen áreas de mejora).

### Módulo I — Configuración de Accesibilidad (2026-06-14)
- Resumen: Conectó el sistema de accesibilidad (`ConfiguracionGlobal`, sin cambios de esquema) al frontend de forma completa. `configuracion/services.py` (nuevo) centraliza validación con `guardar_configuracion` (valida los 7 campos `choices` contra opciones del modelo, acota volumen 0-100, conserva previo si inválido). Amplió plantilla `panel.html` de 4 a 9 controles: se agregaron "Espaciado entre Letras", "Espaciado entre Palabras", "Tipo de Voz" y controles range para "Volumen de Narración"/"Volumen de Música"; se mostraron ahora las 4 opciones de tema completas (infantil-azul, infantil-verde, además de claro/oscuro). Resolvió la deuda de las 7 plantillas standalone (avatar, camara, historias, estadisticas, niveles/mapa, desafio): todas definen ahora un bloque `:root` con CSS vars de accesibilidad (`--global-font-family`, `--global-font-size`, `--global-letter-spacing`, `--global-word-spacing`, colores y fondo) basadas en `config_global` (context processor), reemplazando el token `primaryFijo` por `primary` dinámico; respetan completamente tema, fuente, tamaño y espaciado configurado por usuario. Se agregó link "Configuración" (icono settings) en navbar de `base.html`.
- Archivos tocados: configuracion/services.py (nuevo), configuracion/views.py, configuracion/tests.py (5 tests nuevos), configuracion/templates/configuracion/panel.html, templates/base.html, avatar/templates/avatar/avatar.html, camara_inteligente/templates/camara_inteligente/camara.html, historias/templates/historias/historias.html, estadisticas/templates/estadisticas/estadisticas.html, niveles/templates/niveles/niveles.html, niveles/templates/niveles/mapa.html, desafio/templates/desafio/desafio.html, static/js/configuracion/panel.js (nuevo), static/css/output.css (rebuild).
- Checklist de calidad: cumplido (5 tests nuevos + suite completa 168/168 OK sin regresiones; `ConfiguracionGlobal` sin migración nueva (makemigrations --check limpio); vista `ver_configuracion` con `@login_required`, ~10 líneas, validación silenciosa-defensiva en `services.py`; JS externo sin inline; `ConfiguracionGlobal` en admin con `list_display`; flujo UX de 5 secciones originales mantenido, estructura de las 7 plantillas intacta, solo se agregó bloque `:root` y renombramiento `primaryFijo`→`primary`; recompensas N/A).
- Deuda/seguimientos para módulos futuros:
  - **Audio no aplicado (deuda explícita)**: `velocidad_narracion`, `tipo_voz`, `volumen_narracion` y `volumen_musica` se persisten y son editables en el panel, pero ningún JS lee estos 4 campos todavía (historias, niveles, desafío, cámara usan voz/velocidad/volumen fijos). Aplicarlos a los 4 flujos de audio queda pendiente para un módulo futuro con su propio ciclo de pruebas (riesgo de regresión en módulos ya cerrados).
  - El token Tailwind `primaryFijo: '#3B82F6'` permanece en `tailwind.config.js` por si una plantilla nueva lo necesitara, pero ya no se usa en ningún HTML (verificado).

### Módulo J — Reportes por Email (2026-06-14)
- Resumen: Nueva app `reportes` que implementa envío de correo de progreso al tutor. Modelo `ReporteEnviado` (usuario FK, correo_destino, tipo_envio manual/automático, exitoso, fecha_envio) registra cada intento con migración `0001_initial`. `reportes/services.py` reutiliza `estadisticas.services.construir_contexto_estadisticas` para armar resumen (nivel, racha, puntuación, insignias, etc.), `enviar_reporte_progreso` renderiza templates HTML/texto plano y envía con `EmailMultiAlternatives`. Si usuario sin `correo_tutor`, no se envía ni registra nada (`status: 'sin_correo'`). Errores capturados con `logging.error(..., exc_info=True)`, registran `ReporteEnviado(exitoso=False)`, devuelven `status: 'error'` sin exponer detalles. Vista `enviar_reporte_view` (`@login_required`, `@require_POST`) dispara envío manual, redirige a `estadisticas` con querystring resultado. Plantilla `estadisticas.html` agrega sección con botón "Enviar reporte a mi tutor" (o link a Ajustes si sin correo) y 3 banners de resultado, sin alterar flujo existente (H.1/H.2/H.3). Management command `enviar_reportes_progreso` para lote automático (semanal/mensual), pensado para ejecutarse vía cron externo — **no se instaló Celery ni dependencias nuevas**; comando no se ejecuta automáticamente. `core/settings.py` agregó `DEFAULT_FROM_EMAIL` vía `EMAIL_FROM_DEFAULT` env var sin hardcodear.
- Archivos tocados: reportes/models.py, reportes/admin.py, reportes/services.py, reportes/views.py, reportes/urls.py, reportes/tests.py (6 tests), reportes/migrations/0001_initial.py, reportes/templates/reportes/email_reporte.html, reportes/templates/reportes/email_reporte.txt, reportes/management/commands/enviar_reportes_progreso.py, core/settings.py, core/urls.py, estadisticas/views.py, estadisticas/templates/estadisticas/estadisticas.html.
- Checklist de calidad: cumplido (6 tests nuevos + suite completa 174/174 OK sin regresiones; migración aplicada, makemigrations --check limpio; `enviar_reporte_view` con `@login_required`/`@require_POST`, sin exponer `str(e)`; no hay JS nuevo; `ReporteEnviado` en admin con `list_display`/`list_filter`; flujo UX de estadísticas intacto, cambio puramente aditivo; recompensas N/A módulo notificación).
- Deuda/seguimientos para módulos futuros:
  - Management command `enviar_reportes_progreso` está listo pero requiere que alguien (DevOps/humano) configure un cron externo en producción para ejecutarlo periódicamente (ej. semanal); no hay scheduler dentro de la app (decisión explícita para no introducir Celery/Redis como dependencia en módulo tier Bajo).
  - `DEFAULT_FROM_EMAIL` usa dominio placeholder (`no-responder@dysplay.app`) vía env var; en producción debe configurarse junto con `EMAIL_BACKEND`/credenciales SMTP reales (variables de entorno, sin hardcodear).
  - El reporte por email no respeta tema/fuente del Módulo I (clientes de correo no soportan CSS vars); usa HTML con estilos inline para compatibilidad, decisión correcta pero documentada por si se desea revisar.

### Módulo K — Celebraciones (2026-06-14)
- Resumen: Cierre del Master Plan A→K. Resolvió la deuda de Módulo B: `obtener_insignias_pendientes(usuario)` existía sin invocarse desde vista. Nuevo endpoint `POST /recompensas/insignias-pendientes/` (recompensas/views.py, vista `insignias_pendientes_view`, decoradores `@login_required` + `@require_POST`; recompensas/urls.py nuevo con ruta registrada en core/urls.py) que consume el servicio existente, devuelve JSON con lista de insignias pendientes (nombre/descripción/imagen), marca mostradas en BD. Cambio additivo en templates/base.html: modal oculto `#modal-celebracion-insignia` con animación de confeti CSS pura (@keyframes confetiCaida, clases .confeti-pieza/.animate-confeti, sin afectar :root ni Tailwind). Nuevo static/js/celebraciones/celebraciones.js: al cargar página autenticada, consulta endpoint via fetch (CSRF) y si hay insignias muestra secuencial en modal con confeti; dispara `AVATAR_EVENT` tipo 'insignia_nueva' (reacción ya existente en avatar/reactions.py) para que avatar celebre junto al usuario. Cero modelos nuevos, cero cambios en context processors, flujos UX de D-G respetados, cero nuevas dependencias pip/npm.
- Archivos tocados: recompensas/views.py, recompensas/urls.py (nuevo), core/urls.py, templates/base.html, static/js/celebraciones/celebraciones.js (nuevo), recompensas/tests.py.
- Checklist de calidad: cumplido (4 tests nuevos + suite completa 178/178 OK; N/A modelos/migraciones; vistas con `@login_required` sin exponer `str(e)`; JS externo sin inline salvo `json_script`; flujo UX templates existentes respetado; recompensas (insignias) otorgadas y validadas 100% server-side).

### Mejora B.6 — Rediseño Visual del Mapa de Niveles (Candy Crush) (2026-06-16)
- Resumen: Rediseño visual completo de `mapa.html` al estilo Candy Crush: cada zona temática cuenta con tarjeta de gradiente (verde/piedra/ámbar/púrpura/índigo según la zona), banner con SVG de ícono temático y fondo degradado. Niveles dispuestos en ruta serpentina con conectores curvos alternados mediante CSS (`rounded-br-[2rem]` / `rounded-bl-[2rem]`). Burbujas diferenciadas por estado: completado (verde 68px + estrella), actual (azul 84px + bounce + badge "¡Toca para jugar!"), bloqueado (gris 60px). Incluye hook `mapa-tema-navidad` para temas de evento futuros. Se agregó Lottie Web CDN a `niveles.html` para soporte de animaciones vectoriales. Suite de tests: 40/40 en verde.
- Archivos tocados: niveles/templates/niveles/mapa.html (reescritura completa), niveles/templates/niveles/niveles.html (script CDN lottie-web 5.12.2), static/css/output.css (rebuild Tailwind).
- Checklist de calidad: cumplido (modelos sin cambios; vistas @login_required; JS sin inline; 40 tests OK; admin N/A; flujo UX respetado; recompensas validadas server-side).

### Mapa de niveles: mapa vivo, responsivo, estrellas y bloqueo de zonas (2026-06-17)
- Resumen: continuación del rediseño del mapa único (canvas continuo, B.6). Se agregaron capas de profundidad (nubes SVG, decoraciones de suelo/vuelo con sombra de apoyo, glow en nodos), microanimaciones desfasadas (balanceo de árboles, vaivén floral, vuelo de mariposa/hada, parpadeo de búho, flotación sutil de nodos) y las ilustraciones reales del Bosque Encantado (`static/images/bosque_encantado/`: arbol, flores, piedras, casa, hongo, buho, mariposa, hada) rotando junto al fallback de emoji para zonas sin arte propio. El mapa se hizo responsivo: `ajustarEscalaMapa()` en `niveles.js` mide el ancho real disponible y escala el canvas (390px de diseño interno) con `transform:scale()` — se resolvió por JS en vez de CSS container queries porque no todos los navegadores las soportan (causaba que el mapa se viera sin escalar en algunos). Se corrigieron tres bugs de navegación: el botón de salir usaba `onclick` inline apuntando a una función no expuesta globalmente (nunca funcionaba); la página no centraba el scroll en el nivel actual al cargar ni al volver de un ejercicio; el header (Menú/monedas) se perdía al hacer scroll en mapas largos (ahora `sticky`). Se agregó el modelo `ProgresoNivel` (mejor resultado en estrellas por nivel) para que el badge del nodo completado muestre 1-3 estrellas reales en vez de una fija. Se agregó el modelo `Zona` (independiente de `Nivel.zona`, que se mantuvo como CharField para no romper `desafio`/`estadisticas`) con bloqueo de creación de niveles nuevos en zonas marcadas `cerrada` (validación en `Nivel.clean()`, capa de usabilidad en el admin). Se agregaron 50 niveles de prueba (10 por zona) para visibilidad. Suite completa del proyecto: 204/204 en verde.
- Archivos tocados: niveles/models.py (`ProgresoNivel`, `Zona`, `Nivel.clean()`), niveles/migrations/0007-0009 (nuevas, additivas), niveles/services.py (decoraciones/nubes/escalas por fase determinista, registro de estrellas, `mejores_estrellas` en el mapa), niveles/admin.py (`NivelAdminForm`, `ZonaAdmin`), niveles/views.py, niveles/templates/niveles/mapa.html, niveles/templates/niveles/niveles.html, static/js/niveles.js (`ajustarEscalaMapa`, `centrarEnNivelActual`, fix botón salir), static/images/bosque_encantado/ (8 SVG nuevos), niveles/tests.py (+19 tests).
- Checklist de calidad: cumplido (migraciones additivas, sin tocar `UsuarioCustom`/`Avatar`/`ConfiguracionGlobal`/campos existentes de `ProgresoEstudiante`; vistas `@login_required` sin exponer errores internos; JS en `niveles.js`, sin inline; 19 tests nuevos + suite completa 204/204 OK; modelos en admin con `list_display`; flujo UX respetado; estrellas/monedas validadas server-side).

### Módulos F y G — Integración Azure OpenAI (2026-06-18)
- Resumen: Se integró Azure OpenAI en tres piezas coordinadas: G.1 frase generada dinámicamente (generar_frase_llm en camara_inteligente/services.py, cacheada 24h por nivel/objeto para reducir costo/latencia, con fallback a banco fijo FraseTemplate→frase genérica si API falla); F.1 generación permanente en admin (nueva acción en historias/admin.py, el administrador da tema y Azure OpenAI genera Historia completa con FragmentoHistoria+OpcionRespuesta, editable como historia curada normal); F.2 generación efímera por el niño (nuevos modelos HistoriaGenerada/FragmentoGenerado/OpcionGenerada separados del sistema curado, nueva UI en historias.html con "Crear mi propia historia", límite 5/24h por usuario + 200/24h global validados en servidor antes de llamar a IA, palabras clave con allowlist de caracteres + longitud máxima anti-inyección, historias expiran 24h sin otorgar monedas/insignias/registro en estadísticas para evitar farmeo, comando limpiar_historias_generadas para cleanup físico). Todos los flujos mantienen fallbacks: si Azure OpenAI no responde, historias curadas y cámara siguen funcionando normalmente. Recompensas validadas 100% server-side sin cambios a modelos compartidos (UsuarioCustom/Avatar/ConfiguracionGlobal intactos).
- Archivos tocados: camara_inteligente/services.py (generar_frase_llm, cache), camara_inteligente/tests.py; historias/models.py (HistoriaGenerada, FragmentoGenerado, OpcionGenerada), historias/services.py (generar_historia_completa_ia, crear_historia_desde_ia, nuevas funciones de generación + límites), historias/admin.py (nueva acción generación LLM), historias/views.py (generación efímera endpoint), historias/urls.py (nueva ruta), historias/management/commands/limpiar_historias_generadas.py (nuevo), historias/templates/historias/historias.html (nueva sección UI), static/js/historias/historias.js (manejo generación), historias/tests.py (39 tests nuevos totales); requirements.txt (openai==2.43.0 + deps transitivas jiter/tqdm).
- Checklist de calidad: cumplido (migraciones aplicadas, vistas @login_required sin exponer str(e), JS externo sin inline, 39 tests nuevos + suite completa 224/224 OK sin regresiones, modelos en admin, flujo UX respetado, recompensas validadas server-side sin tocar modelos compartidos).

### Módulo G — Rediseño: Generación de frases para cualquier objeto vía IA (2026-06-18)
- Resumen: Se extendió la capacidad de generación de frases de la cámara para trabajar con CUALQUIER objeto detectado por Google Vision, eliminando la limitación del diccionario fijo `TRADUCCION_OBJETOS`. La función `generar_frase_objeto` ahora intenta primero traducir y generar la frase vía Azure OpenAI (nueva función `generar_objeto_y_frase_llm`), reutilizando el patrón few-shot comprobado en historias.services; `TRADUCCION_OBJETOS` y `FraseTemplate` pasaron a ser un respaldo de emergencia si el LLM falla (timeout, cuota, etc.), no el límite de vocabulario. La cache (TTL 24h) se indexa ahora por etiqueta en inglés + nivel, de modo que "la misma palabra detectada → misma frase en 24h" se cumple para cualquier objeto. Se actualizó el help_text de `FraseTemplate.objeto_keyword` para reflejar su nuevo rol de respaldo. Se agregó migración `0002_alter_frasetemplate_objeto_keyword.py` (solo cambio de metadata, sin impacto en datos). Se actualizaron y agregaron tests verificando que objetos fuera del diccionario (ej. "dolphin") generen objeto+frase correctamente vía LLM.
- Archivos tocados: camara_inteligente/services.py, camara_inteligente/models.py, camara_inteligente/migrations/0002_alter_frasetemplate_objeto_keyword.py, camara_inteligente/tests.py.
- Checklist de calidad: cumplido (migraciones aplicadas solo metadata, vistas @login_required sin exponer str(e), JS sin cambios, >= 3 tests nuevos en verde, suite completa 226/226 OK, modelo en admin, flujo UX respetado, monedas validadas server-side).

### Mejora E — Personalización de Ejercicios del Desafío Diario por Nivel del Estudiante (2026-06-18)
- Resumen: Se refactorizó el flujo de selección de ejercicios del Desafío Diario para personalizarse según el nivel actual del estudiante en vez de usar un pool aleatorio compartido por todos los usuarios. Antes, `DesafioDiario` almacenaba ejercicios obligatorios/opcionales (M2M) generados una sola vez al crear el desafío; ahora permanece sin datos, y los ejercicios se calculan dinámicamente por usuario mediante `_obtener_ejercicios_desafio(usuario, desafio)` que filtra `MisionVocabulario` a `nivel__numero__lte=nivel_usuario` (con fallback al catálogo completo si el estudiante no tiene misiones en su rango). La selección es determinista por `random.Random(seed=(usuario.pk, desafio.fecha))` (sin necesidad de persistir), igual a patrones probados en otros módulos. Se eliminó la función de auto-asignación aleatoria al crear el desafío (`_asignar_ejercicios_aleatorios`). Se actualizó el help_text de los campos M2M para reflejar su nuevo rol de override editorial opcional (admin puede asignar manualmente si desea un desafío especial, de lo contrario se usa personalización). `construir_estado_desafio` y `procesar_intento_desafio` ahora consultan `_obtener_ejercicios_desafio` y validan anti-tampering del `mision_id` enviado contra la lista calculada.
- Archivos tocados: desafio/services.py, desafio/models.py, desafio/migrations/0002_alter_desafiodiario_ejercicios_obligatorios_and_more.py, desafio/tests.py.
- Checklist de calidad: cumplido (migraciones aplicadas sin impacto en datos, vistas @login_required sin exponer str(e), JS sin cambios, 22 tests en verde (agregados 6 tests de personalización, eliminado 1 obsoleto, ajustado 1 existente), suite completa 232/232 OK sin regresiones, modelo en admin, flujo UX de templates respetado, recompensas validadas server-side igual que antes).

### Deuda Técnica — Aplicación Real de Audio a Narraciones (2026-06-19)
- Resumen: Se conectaron 3 de los 4 campos de audio de `ConfiguracionGlobal` (`velocidad_narracion`, `tipo_voz`, `volumen_narracion`) al TTS real (`SpeechSynthesisUtterance`) en historias, niveles, desafío diario y cámara inteligente. Antes estaban persistidos/editables en el panel de configuración pero ningún JS los leía (rate fijo 0.85, sin voice, sin volume en las 4 implementaciones). Nuevo context processor aditivo `config_audio_json` (dict con los 3 campos + defaults para anónimos), nuevo helper `window.crearUtteranceConfigurada(texto, lang)` en `static/js/utils/audio_narracion.js` que mapea: velocidad_narracion→rate (lenta=0.5/normal=0.85/rapida=1.2), volumen_narracion/100→volume nativo, selecciona voice best-effort por idioma es-* + heurística género vía `speechSynthesis.getVoices()`, fallback seguro sin excepciones. 4 templates `historias.html`, `niveles.html`, `desafio.html`, `camara.html` agregan `{{ config_audio_json|json_script:"config-audio" }}` + `<script src=".../audio_narracion.js">` antes de JS de módulo. 4 archivos JS reemplazan construcción fija por llamada al helper, manteniendo `lang='es-EC'`. `volumen_musica` queda sin uso, documentado como estado preexistente (ningún módulo tiene música de fondo; campo reservado futuro, no es deuda nueva).
- Archivos tocados: configuracion/context_processors.py, static/js/utils/audio_narracion.js (nuevo), historias/templates/historias/historias.html, niveles/templates/niveles/niveles.html, desafio/templates/desafio/desafio.html, camara_inteligente/templates/camara_inteligente/camara.html, static/js/historias/historias.js, static/js/niveles.js, static/js/desafio/desafio.js, static/js/camara_inteligente/camara.js.
- Checklist de calidad: cumplido (sin cambios de modelos ni migraciones, context processor solo aditivo (nueva clave `config_audio_json`, clave `config_global` intacta), JS en archivos .js separados sin inline, vistas no tocadas, suite completa 232/232 OK antes y después sin regresiones, sin exponer errores internos al cliente (try/catch silencioso en JS nuevo), sin nuevas dependencias pip/npm, flujo UX de templates respetado).

### Incremento Módulo I — Narración TTS Dual: Azure Speech Neural + Fallback Web Speech API (2026-06-19)
- Resumen: Implementación de narración TTS dual motor para accesibilidad mejorada. Se agregó campo `motor_voz` a `ConfiguracionGlobal` (choices: navegador/azure) permitiendo al usuario elegir entre Web Speech API del navegador (gratis, instantáneo, voces limitadas/robóticas) y Azure Speech TTS neural (es-EC-LuisNeural/es-EC-AndreaNeural con SSML para pitch/rate/volumen personalizado). Nuevo endpoint `configuracion:sintetizar_audio` (POST, `@login_required`, validación de 500 caracteres con rechazo HTTP 400, escape XML antes de SSML) que llama a `servicios/utils.py:sintetizar_voz_azure`. Nueva función `window.narrarTexto()` en `static/js/utils/audio_narracion.js` que intenta Azure primero; si falla (sin credenciales, timeout, error de red), fallback automático a `SpeechSynthesisUtterance` del navegador, sin bloquear flujo del usuario. Los 4 módulos con narración (niveles, desafío, historias, cámara) integran `window.narrarTexto()` deshablicitando el botón "Escuchar" durante síntesis para evitar duplicación de llamadas a API de pago.
- Archivos tocados: configuracion/models.py (campo motor_voz aditivo), configuracion/migrations/0002_configuracionglobal_motor_voz.py (nueva, aditiva), configuracion/services.py (validación), configuracion/context_processors.py (nueva clave motor_voz_json), configuracion/views.py (nueva vista sintetizar_audio), configuracion/urls.py (nueva ruta configuracion:sintetizar_audio), configuracion/templates/configuracion/panel.html (nueva sección Motor de Voz), configuracion/tests.py (+5 tests), servicios/utils.py (nueva función sintetizar_voz_azure), static/js/utils/audio_narracion.js (nueva función window.narrarTexto), niveles/templates/niveles/niveles.html, desafio/templates/desafio/desafio.html, historias/templates/historias/historias.html, camara_inteligente/templates/camara_inteligente/camara.html, static/js/niveles.js, static/js/desafio/desafio.js, static/js/historias/historias.js, static/js/camara_inteligente/camara.js.
- Checklist de calidad: cumplido (migración aditiva aplicada sin cambios destructivos, vistas con @login_required sin exponer errores internos, JS en archivos .js separados sin inline, 5 tests nuevos + suite completa 237/237 OK sin regresiones, ConfiguracionGlobal en admin con nuevo campo, flujo UX respetado, recompensas N/A).
- Nota de deuda técnica: endpoint `sintetizar_audio` no usa `@require_POST`/`require_http_methods` explícito (minor gap, no bloqueante).

### Módulo G — Rediseño Completo: Trabalenguas Educativo, Google Vision Ampliado, Detección Priorizada e Overlay Visual (2026-06-20)
- Resumen: Rediseño integral de la cámara inteligente: (1) Trabalenguas en vez de frase simple vía nueva función `generar_objeto_y_trabalengua_llm` (genera texto educativo corto vía Azure OpenAI, reutiliza cache por objeto+nivel, fallback a banco fijo si LLM falla). (2) Google Cloud Vision extendido: `analizar_imagen_google_vision` ahora incluye `OBJECT_LOCALIZATION` (con bounding box normalizado 0-1), `LOGO_DETECTION` y `TEXT_DETECTION` en una sola request, devolviendo `etiquetas`/`logos`/`texto` separados. (3) Priorización inteligente: nueva función `_detectar_fuente_principal` que elige eje del trabalenguas en orden texto legible → logo conocido → objeto localizado, centralizando la lógica en `generar_trabalengua_deteccion` (orquestadora que reemplaza a `generar_frase_objeto`). (4) Overlay visual en vivo: Canvas 2D puro mientras el niño apunta (contorno genérico + sombreado suave), resaltado de confirmación sobre la foto capturada (bounding box exacto devuelto por Vision, color según fuente) antes de pasar al ejercicio, sin vendorizar OpenCV.js (8MB innecesarios tras evaluación). (5) API refactorizada: `/camara/capturar/` devuelve `trabalengua_generado`, `fuente_deteccion`, `caja_deteccion`, `caja_normalizada`; `/camara/evaluar/` espera parámetro `trabalengua_referencia` (antes `frase_referencia`).
- Archivos tocados: servicios/utils.py (analizar_imagen_google_vision con OBJECT_LOCALIZATION/LOGO_DETECTION/TEXT_DETECTION), camara_inteligente/services.py (generar_objeto_y_trabalengua_llm, _detectar_fuente_principal, generar_trabalengua_deteccion, orquestación completa), camara_inteligente/views.py (rename parámetro trabalengua_referencia), camara_inteligente/models.py (help_text de FraseTemplate.frase_plantilla), camara_inteligente/migrations/0003_alter_frasetemplate_frase_plantilla.py (nuevo), camara_inteligente/tests.py (suite actualizada a 35 tests), static/js/camara_inteligente/camara.js (overlay Canvas 2D en vivo, caja de confirmación, rename campos JSON), camara_inteligente/templates/camara_inteligente/camara.html (nuevo canvas overlay, textos actualizados).
- Checklist de calidad: cumplido (migraciones aplicadas solo help_text, vistas @login_required sin exponer str(e), JS en archivo separado sin inline, 35 tests del módulo + suite completa 246/246 OK sin regresiones, FraseTemplate en admin, flujo UX de 5 pasos respetado—capturaMenu→visor→análisis→overlay+confirmación→ejercicio→resultado—, recompensas otorgadas y validadas 100% server-side con campo trabalengua_referencia exacto).
- Deuda/seguimientos para módulos futuros:
  - Bounding boxes de logos en Vision (LOGO_DETECTION) generalmente tienen baja confianza; evaluación en campo necesaria para confirmar si el overlay de logo es útil o si requiere ajuste de umbral.
  - `_detectar_fuente_principal` actualmente prioriza "texto legible con alta confianza" primero; si muchos estudiantes ven fondos con tipografía (señales, anuncios), considerar filtro por región (texto centrado) o área en actualizaciones futuras.
  - Canvas overlay se evalúa con navegadores desktop/móvil reales; WebGL o Shader performance queda como opción futura si latencia de síntesis de trabalenguas causa bloqueo perceptible en el overlay.

### Módulo G — Fix Correctivo: Priorización Correcta de Objeto Base vs. Calificadores Textuales (2026-06-20)
- Resumen: Corrección del bug reportado donde apuntar a una goma de borrar generaba trabalengua con código de serie ("703064 44750") en vez de reconocer el objeto. Causa raíz: la lógica anterior de `_detectar_fuente_principal` priorizaba CUALQUIER texto detectado (`TEXT_DETECTION`) por sobre el objeto localizado (`OBJECT_LOCALIZATION`), sin validar si el texto era una palabra real ni si estaba físicamente sobre el objeto. Fix: refactorización completa de la detección en `camara_inteligente/services.py`. El objeto localizado es SIEMPRE la base del trabalengua; un logo o texto solo actúa como "calificador" enriquecedor (ej. "botella" + etiqueta "agua" → "botella de agua"; "botella" + logo "Coca-Cola" → "botella de Coca-Cola") si se cumplen dos condiciones: (a) solapamiento espacial: nueva función `_caja_contiene_centro` verifica si el centro del logo/texto cae dentro del bounding box del objeto con margen de tolerancia 15%, y (b) si es texto, debe parecer palabra real no código: nueva función `_texto_es_calificador_valido` rechaza texto >30 caracteres o con >30% dígitos (detecta códigos de serie, MACs, números). Función nueva `_detectar_objeto_y_calificador` reemplaza a la anterior `_detectar_fuente_principal`. Cambio de contrato en `servicios/utils.py`: `analizar_imagen_google_vision` ahora normaliza vértices de `logos` y `texto` a fracciones 0-1 (antes en píxeles), decodificando dimensiones reales con Pillow (dependencia preexistente), para que todas las cajas vivan en el mismo sistema de coordenadas. Cambio de firma en `generar_objeto_y_trabalengua_llm`: antes `(valor, fuente, nivel_usuario)`, ahora `(etiqueta_en, calificador, nivel_usuario)` donde `etiqueta_en` es siempre la etiqueta del objeto y `calificador` es el enriquecedor opcional (sin traducción). Endpoint `/camara/capturar/`: respuesta JSON cambió clave `fuente_deteccion` por `fuente_calificador` (valores: `'logo'`, `'texto'`, `None`), eliminó `caja_normalizada` (redundante, todas normalizadas siempre). `static/js/camara_inteligente/camara.js` actualizado para reflejar los cambios de JSON.
- Archivos tocados: `servicios/utils.py`, `camara_inteligente/services.py`, `static/js/camara_inteligente/camara.js`, `camara_inteligente/tests.py` (clases `TextoEsCalificadorValidoTests`, `CajaContieneCentroTests`, `DetectarObjetoYCalificadorTests` incluyendo el caso exacto del bug de goma+código de serie).
- Checklist de calidad: cumplido (sin migraciones nuevas, 46 tests del módulo + 257 tests de suite completa en verde, sin exponer errores internos al cliente, flujo UX no alterado, solo lógica interna de detección mejorada).

### Módulo G — Fix Correctivo Segunda Pasada: Artefactos no Significativos de Vision (Códigos de Barras, Etiquetas) (2026-06-20)
- Resumen: Segunda corrección tras prueba de campo: apuntar a un borrador con código de barras impreso generó trabalengua "¡Qué curioso! Veo algo llamado 1d barcode..." — el problema no era un calificador de texto/logo sin sentido, sino que `OBJECT_LOCALIZATION` de Google Vision identificó el código de barras como el "objeto" principal (score más alto que el objeto real), en vez del borrador. Fix: refactorización de `_detectar_objeto_y_calificador` en `camara_inteligente/services.py`. Se agregó `analizar_imagen_google_vision` con nueva feature `LABEL_DETECTION` (etiquetas generales sobre la imagen completa, sin bounding box localizado), devuelta en clave nueva `etiquetas_generales` del dict de retorno. Nueva constante `ETIQUETAS_OBJETO_IGNORADAS` (denylist: '1d barcode', '2d barcode', 'barcode', 'qr code', 'label', 'sticker', etc.) y función `_es_etiqueta_objeto_valida` que filtra etiquetas artefacto. Flujo mejorado: primero se filtran etiquetas localizadas (`OBJECT_LOCALIZATION`) rechazando artefactos; si quedan, se elige la de mayor score; si ninguna supera el filtro, se cae a `etiquetas_generales` (`LABEL_DETECTION`) como respaldo — en ese caso no hay bounding box normalizado (`vertices: None`), solo la etiqueta genérica; si tampoco hay nada en `etiquetas_generales`, se retorna `None` (sin nada que practicar). Pipeline de trabalengua intacto: consume `etiqueta_en` del resultado filtrado e ignora dónde proviene (localizado vs. general).
- Archivos tocados: `servicios/utils.py` (feature `LABEL_DETECTION` en `analizar_imagen_google_vision`), `camara_inteligente/services.py` (constante `ETIQUETAS_OBJETO_IGNORADAS`, función `_es_etiqueta_objeto_valida`, refactorización de `_detectar_objeto_y_calificador`), `camara_inteligente/tests.py` (4 tests nuevos en `DetectarObjetoYCalificadorTests`: caso "1D Barcode" con score mayor que "Eraser", validación de denylist, fallback a etiquetas generales, manejo de imagen sin objetos válidos).
- Checklist de calidad: cumplido (sin migraciones nuevas, sin nuevas dependencias externas—LABEL_DETECTION es parte de Google Cloud Vision ya integrada—, 49 tests del módulo + 260 tests de suite completa en verde, sin exponer errores internos al cliente, flujo UX no alterado, recompensas validadas igual que antes).

### Módulo G — Mejora UX: Punto Central con Detección Dinámica en Vivo y Validación de Objetivo (2026-06-20)
- Resumen: Reemplazo del efecto visual de sombreado en vivo (diferencia de frames en escala de grises) por un sistema de confirmación inteligente: punto/círculo fijo en el centro del visor + canvas dinámico que resalta con marco grande el objeto detectado más cercano a ese punto, actualizándose cada ~280ms mientras el niño apunta la cámara. Se vendorizó TensorFlow.js + COCO-SSD (lite_mobilenet_v2, ~18MB total) en `static/js/vendor/` con carga diferida (scripts inyectados solo al entrar al visor, nunca en el bundle global) y degradación limpia si el modelo no carga. Backend: nueva función `_sanear_punto_objetivo` (valida/acota el punto normalizado recibido, nunca rompe el flujo), `_elegir_etiqueta_principal` (si el punto central del visor cae dentro de la caja de detección de Google Vision, se prioriza esa etiqueta sobre la de mayor confianza global), y parámetro opcional `punto_objetivo` en `procesar_captura_imagen`/`_detectar_objeto_y_calificador`/`generar_trabalengua_deteccion` (default `None`, preserva compatibilidad). Endpoint `/camara/capturar/` espera campo adicional `punto_objetivo_json` (`{x,y}` normalizado 0-1, ignorado silenciosamente si ausente/malformado). Frontend: `static/js/camara_inteligente/camara.js` + `camara.html` agregan punto visual fijo (`#punto-central-mira`) y canvas de detección (`#canvas-deteccion-vivo`) que dibuja marco sobre la caja de COCO-SSD más cercana al punto, con guard anti-acumulación `deteccionEnCurso` para dispositivos lentos.
- Archivos tocados: `static/js/vendor/tf.min.js`, `static/js/vendor/coco-ssd.min.js`, `static/js/vendor/coco-ssd-model/` (3 archivos JSON/bin), `static/js/camara_inteligente/camara.js`, `camara_inteligente/templates/camara_inteligente/camara.html`, `camara_inteligente/services.py`, `camara_inteligente/views.py`, `camara_inteligente/tests.py` (10 tests nuevos: saneo de punto_objetivo, prioridad por punto sobre score global, comportamiento idéntico si punto ausente/malformado).
- Checklist de calidad: cumplido (sin cambios de modelos/migraciones, nueva dependencia JS vendorizada (no CDN) con aprobación explícita del usuario tras validar tamaño real, 57 tests del módulo + 268 tests de suite completa en verde sin regresiones, flujo UX de captura intacto—mismo botón, mismas 5 vistas—, degradación limpia si modelo TensorFlow no carga, recompensas validadas server-side con punto_objetivo ignorado seguro si inválido/ausente).

### Módulo G — Fix Correctivo Tercera Pasada: Descomposición de Traducción y Generación (2026-06-20)
- Resumen: Corrección arquitectónica del acoplamiento crítico en `generar_objeto_y_trabalengua_llm`, causante de fallos silenciosos en objetos fuera del diccionario. Problema raíz: una sola llamada a Azure OpenAI (modelo Phi-4-mini-instruct documentado como poco confiable) intentaba 3 tareas a la vez (traducir + generar trabalengua + responder JSON), y si cualquiera fallaba, se perdía TODO incluyendo la traducción simple (tamaño fundamental que el modelo suele acertar aislado). Solución: descomposición en dos funciones independientes, cada una con su propia llamada simple al LLM en texto plano: `_traducir_objeto_llm(etiqueta_en, calificador)` (solo traduce el objeto al español, combinándolo con calificador si existe, sin traducir el calificador) + `_generar_trabalengua_llm(objeto_es, nivel_usuario)` (recibe el objeto YA en español, genera trabajo educativo). Cacheo separado por objeto/nivel en ambos casos. Nueva orquestadora `generar_trabalengua_deteccion` que intenta primero traducción LLM; si falla, cae al diccionario `TRADUCCION_OBJETOS` (ahora respaldo de última instancia, no camino principal). Con el nombre del objeto en español resuelto (LLM o diccionario), intenta generar trabajo; si falla, busca `FraseTemplate` o usa fallback genérico — pero SIEMPRE con el nombre en español correcto, incluso para objetos fuera del diccionario, porque traducción y generación ya no están acopladas.
- Archivos tocados: camara_inteligente/services.py (_traducir_objeto_llm, _generar_trabalengua_llm nuevas, generar_trabalengua_deteccion refactorizada), camara_inteligente/tests.py (clases TraducirObjetoLlmTests y GenerarTrabalenguaLlmTests reemplazando GenerarObjetoYTrabalenguaLlmTests, test específico del caso reportado: traducción LLM correcta + generación trabajo fallida, verificando nombre en español en resultado final).
- Checklist de calidad: cumplido (sin migraciones nuevas, sin nueva dependencia externa, 62 tests del módulo + 273 tests de suite completa en verde sin regresiones, vistas @login_required intactas, JS sin cambios, flujo UX respetado, recompensas validadas server-side idéntico a antes).

### Módulo G — Fix Correctivo Cuarta Pasada: Detección y Rechazo de Repetición Degenerada de Palavras (2026-06-20)
- Resumen: Corrección de estabilidad del modelo LLM (Phi-4-mini-instruct) reportada por usuario al probar cámara con un peine: genera trabajo "Pelo del perro con el peine de plata, peina peina peina peina peina, peina peina peina peina peina", donde el LLM "se atascó" repitiendo mecánicamente la palabra "peina" 5+ veces consecutivas (patrón de degeneración conocido en modelos pequeños al generar texto con repetición). Fix: nueva función validación `_tiene_repeticion_degenerada(texto)` en `camara_inteligente/services.py` que detecta si alguna palabra aparece >3 veces seguidas consecutivas; permite repetición natural de 2-3 veces (ej. "pule, pule, pule" es trabajo válido), pero rechaza cadenas mecánicas de 4+. Integrada como validación adicional en `_generar_trabalengua_llm`: si el texto generado tiene patrón degenerado se descarta igual que respuesta vacía o demasiado larga, y se cae a fallback `FraseTemplate`/trabajo genérico (dichos fallbacks no tienen este problema). Refuerzos adicionales: se actualizó el prompt del LLM pidiéndole explícitamente no repetir la misma palabra más de dos veces seguidas, mitigación en el origen que no solo depende de validación posterior. Clases de tests nuevas `TieneRepeticionDegeneradaTests` (caso real del peine) + test en `GenerarTrabalenguaLlmTests` verificando que respuesta con degeneración se descarte y se retorne fallback válido.
- Archivos tocados: camara_inteligente/services.py (_tiene_repeticion_degenerada nueva, _generar_trabalengua_llm con validación, actualización de prompt), camara_inteligente/tests.py (clase TieneRepeticionDegeneradaTests, test new en GenerarTrabalenguaLlmTests).
- Checklist de calidad: cumplido (sin migraciones nuevas, sin nueva dependencia externa, 67 tests del módulo + 278 tests de suite completa en verde sin regresiones, vistas @login_required intactas, JS sin cambios, flujo UX respetado, recompensas validadas server-side idéntico a antes).

### Módulo G — Reversión Explícita: Revert de Trabalenguas a Frase Simple (2026-06-20)
- Resumen: Tras verificación de campo exhaustiva del usuario con múltiples objetos (gafas de sol, peine, goma de borrar, etc.), se reportó que el flujo de trabalenguas generados fallaba repetidamente, cayendo a mensajes genéricos de respaldo ("¡Qué curioso! Veo algo llamado gafas de sol...") un 70%+ del tiempo, además de latencia incrementada. Tras discusión de opciones (mantener trabalenguas y seguir depurando LLM, revertir a frase simple, o híbrido), el usuario eligió explícitamente descartar el concepto de trabalenguas introducido esta sesión y volver a una frase corta, natural y confiable. Reversión completa de terminología y contenido: `TRABALENGUA_GENERICA` → `FRASE_GENERICA` (nuevo texto: "¡Qué interesante! Veo algo llamado {objeto}. ¿Puedes describirlo en voz alta?", sin instrucción de repetición); `_generar_trabalengua_llm` → `_generar_frase_llm` (prompt ahora pide frase simple ≤10 palabras en español, natural y educativa, en vez de trabalenguas con repetición de palabra); `generar_trabalengua_deteccion` → `generar_frase_deteccion` (orquestadora renombrada, flujo intacto). Claves de respuesta JSON en endpoints y frontend: `trabalengua_generado` → `frase_generada`; parámetro en vista evaluación: `trabalengua_referencia` → `frase_referencia`. Modelo `FraseTemplate.frase_plantilla`: help_text actualizado para reflejar rol de respaldo (migración `0004`, solo metadata). Se mantienen INTACTAS todas las demás mejoras: arquitectura objeto-base + calificador, separación traducción/generación en dos llamadas LLM, validación `_tiene_repeticion_degenerada` como red de seguridad genérica, punto de apuntado con COCO-SSD, overlay visual, Google Vision extendido (objeto+logo+texto). Suite: 67 tests del módulo + 278 tests de suite completa en verde.
- Archivos tocados: camara_inteligente/services.py (rename: `_generar_trabalengua_llm` → `_generar_frase_llm`, `generar_trabalengua_deteccion` → `generar_frase_deteccion`, cambio de prompt y contenido genérico), camara_inteligente/views.py (rename parámetro `trabalengua_referencia` → `frase_referencia`), camara_inteligente/models.py (FraseTemplate.frase_plantilla help_text), camara_inteligente/migrations/0004_alter_frasetemplate_frase_plantilla.py (nueva, solo metadata), camara_inteligente/templates/camara_inteligente/camara.html (textos y claves JSON), static/js/camara_inteligente/camara.js (rename claves JSON `trabalengua_generado` → `frase_generada`), camara_inteligente/tests.py (clases/aserciones renombradas).
- Checklist de calidad: cumplido (migración aplicada, 67 tests del módulo + 278 tests de suite completa en verde sin regresiones, vistas @login_required intactas, flujo UX no alterado más allá del texto mostrado, recompensas sin cambios de lógica, degradación/fallback intactos).

### Módulo G — Fix Correctivo: Optimización de Timeout y Reintentos en Cliente Azure OpenAI (2026-06-20)
- Resumen: Corrección de latencia extrema (hasta 5 minutos en una captura de "gafas") reportada por usuario, que excedía ampliamente el timeout configurado (`AZURE_OPENAI_TIMEOUT_SEGUNDOS = 8` segundos). Problema raíz: el cliente `AzureOpenAI` en `_traducir_objeto_llm` y `_generar_frase_llm` se construía sin especificar `timeout` ni `max_retries` a nivel de cliente, heredando valores por defecto del SDK (`max_retries=2`, reintentos automáticos en timeouts/errores de conexión, timeout de cliente mucho mayor a los 8 segundos del override en `.create()`). Los reintentos internos del SDK, combinados con posible latencia real de red o cold-start del servidor Azure, multiplicaban el tiempo de espera del niño frente a la cámara sin aporte de valor (el proyecto ya tiene capas de respaldo: LLM → diccionario → frase genérica). Solución: configuración explícita del cliente con `timeout=AZURE_OPENAI_TIMEOUT_SEGUNDOS` a nivel constructor (no solo en llamada) y `max_retries=0` (sin reintentos automáticos del SDK). Se agregó medición con `time.monotonic()` en logs de error de ambas funciones para diagnosticar con datos reales si lentitud persiste. Tras cambio, latencia se normalizó a 8-12 segundos máximo (8s timeout + pequeño overhead de inicialización/validación). Nota de seguimiento: si lentitud reaparece, el origen ya no sería el código sino latencia real del deployment de Azure OpenAI (Phi-4-mini-instruct) — medir con nuevos logs antes de seguir ajustando.
- Archivos tocados: camara_inteligente/services.py (_traducir_objeto_llm, _generar_frase_llm con timeout/max_retries a nivel cliente, `time.monotonic()` en logs de error).
- Checklist de calidad: cumplido (sin migraciones nuevas, sin nueva dependencia externa, 67 tests del módulo + 278 tests de suite completa en verde sin regresiones, vistas @login_required intactas, JS sin cambios, flujo UX respetado, recompensas validadas server-side idéntico a antes).

### Módulo G — Mejora: Control de Costos de IA y Variedad de Contenido (2026-06-20)
- Resumen: Implementación de funcionalidades de control de costos y variedad de contenido en la cámara inteligente. (1) Nuevo modelo `ConfiguracionCamara` (singleton, `pk=1` forzado en `save()`) con campo booleano `modo_economico` editable desde admin, permitiendo desactivar por completo las llamadas a Azure OpenAI cuando presupuesto de IA sea crítico. (2) Campo `creada_automaticamente` agregado a `FraseTemplate` para distinguir frases manuales (equipo docente) vs. generadas automáticamente (sistema). (3) Auto-guardado automático de variantes: cada frase generada con éxito por LLM se guarda como `FraseTemplate(creada_automaticamente=True)` para reutilización futura, con tope de 5 variantes por combinación objeto/nivel (`MAXIMO_VARIANTES_FRASE_AUTOGUARDADA`), evitando dependencia 100% del LLM a largo plazo. (4) Modo económico activa fallback automático: apaga Azure OpenAI, frase sale de diccionario `TRADUCCION_OBJETOS` + `FraseTemplate` existentes, con plantilla de última barrera `FRASE_SOLO_NOMBRE` ("Este objeto se llama {objeto}. Dilo en voz alta: {objeto}.") si no hay ninguna guardada, reduciendo costo a cero tras primer uso. (5) Echo de corto plazo (15 min, `CACHE_ECO_CAMARA_TIMEOUT_SEGUNDOS`): antes de cualquier llamada, se verifica si estudiante detectó la misma etiqueta hace poco; si es así, repite frase en caché sin tocar Vision-translation ni LLM, con clave de caché por `usuario_id` (dos estudiantes simultáneos no comparten).
- Archivos tocados: camara_inteligente/models.py, camara_inteligente/migrations/0005_configuracioncamara_and_more.py (nuevo), camara_inteligente/admin.py, camara_inteligente/services.py (`generar_frase_deteccion` ganó `usuario_id`, nuevas funciones `_clave_cache_eco`, `_resolver_frase_respaldo`, `_guardar_frase_template_automatica`), camara_inteligente/tests.py (15 tests nuevos).
- Checklist de calidad: cumplido (migración aplicada sin tocar modelos compartidos, 80 tests del módulo + 291 tests de suite completa en verde, admin con `list_display` y permisos de singleton, flujo UX de captura sin cambios visibles para estudiante, recompensas validadas server-side).

### Módulo H — Mejora de Interactividad y UX del Panel de Estadísticas (2026-06-20)
- Resumen: Rediseño de interactividad del módulo de estadísticas para mantener el interés de los niños. (1) Racha interactiva: al tocar la tarjeta de racha, el ícono de llama anima y aparece globo motivador aleatorio (5 variantes, con singular/plural de día resuelto), desaparece en 3 segundos, accesible por teclado (Enter/Espacio). (2) Animaciones de carga: barras del gráfico semanal crecen escalonadas desde 0% (efecto ola), número de racha cuenta ascendente animado (0→valor real). (3) Insignias clickeables con confeti reciclado: cada insignia obtenida toca para reabrir modal de celebración con confeti (reutiliza lógica del Módulo K sin duplicar), insignias obtenidas en últimos 3 días muestran badge "¡Nueva!" con brillo (nuevo campo `es_nueva` en `construir_insignias`, constante `DIAS_INSIGNIA_NUEVA`). (4) Pista en coleccionables bloqueados: tocar "???" muestra modal con pista fija sin revelar nombre real (evita espóiler). Decisión explícita: círculo de perfil donde va cara del avatar queda reservado (comentario HTML, sin cambios estructurales, para integración futura del usuario).
- Archivos tocados: estadisticas/services.py (campo `es_nueva` en `construir_insignias`, constante `DIAS_INSIGNIA_NUEVA`), estadisticas/templates/estadisticas/estadisticas.html, static/js/estadisticas/estadisticas.js (nuevo), static/css/output.css (rebuild), estadisticas/tests.py (2 tests nuevos para `es_nueva`).
- Checklist de calidad: cumplido (sin migraciones nuevas, sin nueva dependencia pip/npm, vistas `@login_required` sin exponer errores, JS en archivo separado sin inline, 25 tests del módulo + 293 tests de suite completa en verde, flujo UX respetado—solo se agregó interactividad sin reordenar ni quitar nada—, accesibilidad por teclado en nuevos clickeables).

### Módulo C — Personaje Visual Animado SVG (2026-06-20)
- Resumen: Construcción del personaje 2D completo 100% en SVG inline para reemplazar la imagen PNG estática previa. Nuevo partial `avatar/_svg_personaje.html` con estructura SVG modular (grupos anidados para cabeza/brazos/torso/piernas, pivotes en articulaciones clave para animación futura). Incluye 9 estados de cara (neutral/feliz/triste/celebrando/pensando/sorprendido/preocupado/analizando/explicando), mostrados/ocultados dinámicamente vía CSS según clase `avatar-{emocion}` que `avatar_events.js` ya aplicaba. Animaciones CSS puras (sin reflow): parpadeo automático, respiración leve del torso, balanceo de cabeza, con respeto a `@media (prefers-reduced-motion: no-preference)` para accesibilidad. Nuevo método `saludar()` en `avatar_events.js` que activa clase `avatar-saludando` para animación de saludo (brazo levantado/agitado), llamado desde `mostrarFraseContextual()`. Integración en dos ubicaciones: widget flotante global (`componente.html`, reemplazando `<img id="avatar-layer-body">`) y vista de personalización (`personalizar.html`, reemplazando `<picture>` roto). Prendas equipables (cabello/ropa/calzado/accesorios) se superponen sin cambios vía z-index (contrato de context processor `avatar_equipados`/`avatar_user`/`reacciones_json` intacto).
- Archivos tocados: avatar/templates/avatar/_svg_personaje.html (nuevo), avatar/templates/avatar/componente.html, avatar/templates/avatar/personalizar.html, static/js/avatar_events.js.
- Checklist de calidad: cumplido (sin migraciones de modelos, sin cambios en `services.py`/vistas/tests existentes, cambio puramente frontend/visual, 31 tests de avatar + 293 tests de suite completa en verde, accesibilidad de movimiento (`prefers-reduced-motion`) añadida por primera vez en el proyecto, no se rompió ningún flujo UX existente, recompensas sin cambios).
- Nota explícita: primera pieza de reorganización más amplia del Módulo C que el usuario continuará en sesiones futuras (ítems de armario/casa sin fixtures, posible unificación de catálogos de reacciones, uso del campo `personalidad`).

### Panel de Administración — Gestión de Contenido Unificada (2026-06-22)
- Resumen: Implementación de un panel propio (no el admin nativo de Django) con interfaz infantil consistente con el resto de la app, gateado por `is_staff` (404 sin delatar existencia). Fase 0: cimiento (app `panel_admin`, acceso restringido, layout sidebar). Fase 1: CRUD genérico via registry.py declarativo (~16 modelos de 5 apps: Recompensas/Desafío/Cámara/Niveles/Historias), 2 vistas genéricas reutilizables (Lista/Crear/Editar/Eliminar) en lugar de código repetido. Fase 2: Avatar a medida con nuevo modelo `CaraAvatar` (estado único + imagen + imagen_parpadeo opcional) que reemplaza 9 caras hardcodeadas por datos en BD, con previsualización en vivo (500×500 con grilla). Fase 3 (cierre): correcciones de calidad: bug de 500 cuando `imagen_parpadeo` vacía (solucionado con guardia `{% if %}` + test regresión); JS inline en `avatar_cara_form.html` movido a `panel_admin/static/panel_admin/avatar_cara_preview.js` (json_script para paso de datos); modelo `CaraAvatar` registrado en admin nativo con `list_display`. Decisión arquitectónica: se mantiene solo `is_staff` nativo (sin sistema nuevo de roles/permisos Group/Permission) porque solo el usuario administra hoy el panel.
- Archivos tocados: app nueva `panel_admin/` completa (modelos, vistas genéricas + 2 a medida, templates, JS modular, tests: 24 nuevos); `avatar/models.py` (modelo `CaraAvatar`), `avatar/admin.py`, `avatar/templatetags/avatar_tags.py` (nuevo), `avatar/templates/avatar/_svg_personaje.html` (caras desde BD); `core/settings.py` (INSTALLED_APPS + context_processor), `core/urls.py` (ruta `/panel/`), `templates/base.html` (link condicional `is_staff`, nuevo bloque `{% block avatar_widget %}`).
- Checklist de calidad: cumplido (migraciones aplicadas, acceso restringido equivalente a @login_required vía is_staff, JS en archivos separados sin inline salvo json_script, 24 tests nuevos + suite completa 316/316 OK, modelo CaraAvatar registrado en admin con list_display, flujo UX del juego respetado—solo ocultar widget en panel—, no aplica recompensas para este módulo).
- Tier de riesgo: Alto (toca core/settings.py, core/urls.py, templates/base.html compartidos, pero de forma aditiva y verificada).
